# configs/env_harvest.yaml

small_village:
  grid_size: 10
  num_agents: 5
  max_steps: 500

big_city:
  grid_size: 30
  num_agents: 5
  max_steps: 1000  # Longer episodes needed to find each other randomly

# Add this new section to the file:

scarcity_conflict:
  grid_size: 25         # Leibo used roughly 25x25 (allows territory formation)
  num_agents: 6          # 6 agents on 24x24 is the "Sweet Spot" for conflict
  max_steps: 1000        # Long episodes allow grudges to form
  regrowth_speed: 0.05   # VERY SCARCE. (Half of your current 0.1)

